<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- 1. 恢复最原始的基础主题 -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>

        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="colorAccent">@color/colorAccent</item>

    </style>



    <!-- 2. 恢复昼夜模式支持（彻底修复太阳/月亮切换失效） -->

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">

        <item name="windowActionBar">false</item>

        <item name="windowNoTitle">true</item>

        <!-- 与 SplashScreen 同色，切换时无闪烁 -->

        <item name="android:windowBackground">#f8fafc</item>

    </style>



    <!-- 3. 经典开屏主题：弃用 Theme.SplashScreen（其视图层遮罩导致全屏图不可见），
         直接用 android:windowBackground 渲染全屏高清开屏图 -->
    <style name="AppTheme.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>



</resources>