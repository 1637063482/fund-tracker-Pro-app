<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- 1. 恢复最原始的基础主题 -->

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>

        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="colorAccent">@color/colorAccent</item>

    </style>



    <!-- 2. 恢复昼夜模式支持（彻底修复太阳/月亮切换失效） -->

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">

        <item name="windowActionBar">false</item>

        <item name="windowNoTitle">true</item>

        <!-- 与 SplashScreen 同色，切换时无闪烁 -->

        <item name="android:windowBackground">#f8fafc</item>

    </style>



    <style name="AppTheme.Splash" parent="Theme.SplashScreen">

        <!-- 全屏高清开屏图（xxxhdpi 下 1280x1920），解决图标模糊问题 -->

        <item name="android:windowBackground">@drawable/splash</item>

        <!-- SplashScreen 视图层底色，与 WebView 背景对齐，消除过渡白屏 -->

        <item name="windowSplashScreenBackground">#f8fafc</item>

        <!-- 隐藏系统自带图标，避免遮罩全屏图 -->

        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>

        <!-- 开屏结束后切换回主主题 -->

        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>

        <!-- 零动画时长，原生开屏消失瞬间即见 WebView，无闪烁 -->

        <item name="windowSplashScreenAnimationDuration">0</item>

    </style>



</resources>